<%- include('partials/header') %>

<section class="page-container">
  <div class="page-header">
    <h2 class="page-title">Edit Machine</h2>
  </div>

  <div class="machine-form-card">
    <form method="post" action="/machine/<%= machine._id %>/edit" class="machine-form">

      <div class="form-row">
        <label>Machine Code</label>
        <input type="text" name="code" value="<%= machine.code %>" required>
      </div>

      <div class="form-row">
        <label>Model</label>
        <input type="text" name="model" value="<%= machine.model %>" required>
      </div>

      <div class="form-row">
        <label>Location</label>
        <input type="text" name="location" value="<%= machine.location %>" required>
      </div>

      <hr class="form-divider">

      <h3 class="section-subtitle">⚙️ Ingredients</h3>

      <div id="ingredient-list" class="ingredient-container">
        <% 
          const defaultIngredients = [
            { name: 'Coffee Beans', quantity: 0 },
            { name: 'Sugar', quantity: 0 },
            { name: 'Milk', quantity: 0 },
            { name: 'Chocolate Syrup', quantity: 0 },
            { name: 'Water', quantity: 0 }
          ];
          const ingredientsToShow = machine.ingredients && machine.ingredients.length > 0 
            ? machine.ingredients 
            : defaultIngredients;
        %>
        <% ingredientsToShow.forEach((ing, index) => { %>
          <div class="ingredient-card">
            <div class="ingredient-fields">
              <input type="text" name="ingredients[<%= index %>][name]" value="<%= ing.name %>" placeholder="Ingredient name" required>
              <input type="number" name="ingredients[<%= index %>][quantity]" value="<%= ing.quantity %>" placeholder="Quantity" step="any" required>
            </div>
            <button type="button" class="btn-icon" onclick="removeIngredient(this)" title="Delete Ingredient">🗑</button>
          </div>
        <% }) %>
      </div>

      <div class="action-row">
        <button type="button" class="btn-secondary" onclick="addIngredient()">+ Add Ingredient</button>
      </div>

      <div class="action-buttons">
        <button type="submit" class="btn-primary">💾 Save Changes</button>
        <button type="button" class="btn-secondary" onclick="window.history.back()">↩ Cancel</button>
        <form action="/machines/delete/<%= machine._id %>" method="POST" style="display:inline;">
          <button type="submit" class="btn-danger">🗑 Delete</button>
        </form>
      </div>

    </form>
  </div>
</section>

<script>
  function addIngredient() {
    const container = document.getElementById('ingredient-list');
    const index = container.children.length;
    const div = document.createElement('div');
    div.className = 'ingredient-card';
    div.innerHTML = `
      <div class="ingredient-fields">
        <input type="text" name="ingredients[${index}][name]" placeholder="Ingredient name" required>
        <input type="number" name="ingredients[${index}][quantity]" placeholder="Quantity" step="any" required>
      </div>
      <button type="button" class="btn-icon" onclick="removeIngredient(this)">🗑</button>
    `;
    container.appendChild(div);
  }

  function removeIngredient(button) {
    button.parentElement.remove();
  }
</script>

<style>
  body {
    background-color: #f3f4f6;
    font-family: 'Segoe UI', Roboto, sans-serif;
    color: #222;
  }

  .page-container {
    padding: 20px;
  }

  .page-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 25px;
  }

  .page-title {
    font-size: 1.4rem;
    font-weight: 600;
    color: #333;
  }

  .machine-form-card {
    background: #fff;
    border-radius: 12px;
    padding: 25px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    max-width: 700px;
    margin: auto;
  }

  .form-row {
    display: flex;
    flex-direction: column;
    margin-bottom: 15px;
  }

  .form-row input {
    padding: 10px;
    border-radius: 6px;
    border: 1px solid #ccc;
    font-size: 0.95rem;
    width: 100%;
  }

  .ingredient-container {
    display: flex;
    flex-direction: column;
    gap: 15px;
  }

  .ingredient-card {
    background: #f9fafb;
    border: 1px solid #e5e7eb;
    border-radius: 8px;
    padding: 12px;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 10px;
  }

  .ingredient-fields {
    display: flex;
    flex-wrap: wrap;
    flex: 1;
    gap: 10px;
  }

  .ingredient-fields input {
    flex: 1;
    min-width: 120px;
  }

  .btn-icon {
    background-color: #ef4444;
    color: white;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    padding: 8px 10px;
  }

  .action-buttons {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: 10px;
    margin-top: 20px;
  }

  .btn-primary,
  .btn-secondary,
  .btn-danger {
    padding: 10px 16px;
    border-radius: 8px;
    font-size: 0.95rem;
    font-weight: 500;
    cursor: pointer;
    border: none;
  }

  .btn-primary { background: #2563eb; color: white; }
  .btn-secondary { background: #e5e7eb; color: #111827; }
  .btn-danger { background: #ef4444; color: white; }

  @media (max-width: 600px) {
    .page-container {
      padding: 10px;
    }
    .machine-form-card {
      padding: 20px;
    }
    .ingredient-fields {
      flex-direction: column;
    }
    .btn-primary, .btn-secondary, .btn-danger {
      width: 100%;
    }
  }
</style>

<%- include('partials/footer') %>
